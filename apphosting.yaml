# Cost-safe defaults shared by staging and production App Hosting backends.
# Backend-specific values live in apphosting.<environment>.yaml. App Hosting
# supplies FIREBASE_CONFIG and FIREBASE_WEBAPP_CONFIG automatically.
runConfig:
  cpu: 1
  memoryMiB: 512
  minInstances: 0
  maxInstances: 2
  concurrency: 80
