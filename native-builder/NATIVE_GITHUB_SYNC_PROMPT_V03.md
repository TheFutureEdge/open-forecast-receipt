# Native Builder GitHub synchronization prompt v0.3

Continue in the existing Native Builder project
`9b5dc37e-f409-4f5e-9206-b20d752313a5`.

Connect this project to the public repository:

`https://github.com/TheFutureEdge/open-forecast-receipt`

Pull the reviewed `main` branch into this same Native project. This repository
is the audited continuation of the application originally generated by Native;
do not create a separate application and do not redesign the approved scope.

Required outcome:

1. Preserve the five-asset Batch 6 scope and the existing user experience.
2. Adopt the canonical OFR schema with separate immutable `receiptPayload` and
   mutable `proofEnvelope`.
3. Load all 60 sanitized receipts: 12 individual advisor forecasts for each of
   PepsiCo, NVIDIA, Bitcoin, Alphabet Class C, and SPY.
4. Preserve the exact JSON values, timestamps, stable subject IDs, point-in-time
   market identifiers, receipt digests, and compact projections in GitHub. Do
   not invent or rewrite source data.
5. Preserve stable asset URL slugs separately from mutable ticker symbols. A
   ticker change must not rewrite historical receipts or attestations.
6. Use the real 17-field `viem` ABI encoder/decoder and read-only Base Sepolia
   EAS verifier. Keep the schema UID and all attestation UIDs empty because no
   attestation has been issued yet.
7. Preserve lazy receipt loading and the dependency fixes; do not restore React
   Router or vulnerable package versions.
8. Run the automated tests, production build, dependency audit, and Native QA.
9. Report the pulled commit SHA, every subsequent file changed by Native, and
   any deviation from GitHub before proposing additional work.

Important integrity rules:

- One individual forecast is one receipt and, later, one EAS attestation UID.
- A multi-attestation transaction is transport batching, not one grouped receipt.
- The receipt digest covers `receiptPayload` only.
- Configured web search and observed search execution are separate facts.
- Never include raw model requests or responses, hidden chain-of-thought,
  credentials, private forecasts, or signing keys.
- A later Base Sepolia timestamp proves timestamped integrity from that point;
  it does not prove forecast accuracy or contemporaneous issuance.

If Native can push changes back to GitHub, use a new integration branch rather
than overwriting `main`, so the changes can be reviewed and tested before merge.

Do not register a schema, issue attestations, connect production Firestore,
deploy publicly, or publish additional branches without explicit approval.
