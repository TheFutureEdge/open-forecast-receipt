# Native QA prompt

```text
QA the current Open Forecast Receipt application as a release candidate. Do not
redesign it and do not invent blockchain state. Verify the five-asset/60-receipt
fixture inventory, six declared showcase selections, stable digests, the
individual-receipt Blockchain proof column, responsive and keyboard-accessible
tables, canonical `/manifest/batch-6/assets/:slug` links, receipt routes, full
JSON, path reconstruction, tamper failure, proof limitations, and production
build. Confirm no secret, wallet key, raw model payload, production API, or fake
UID exists. Report exact failures with file and route evidence. If a fix is
required, hand back to Builder with the smallest specific change; otherwise
mark the Native-hosted release candidate ready.
```
